>Run api with the following in the cmd
uvicorn Pi_API:app --reload or click it

>API includes a speech to speech to text feature
"http://{address}:4000/api"

>API includes a ping feature
"http://{address}:4000/api/ping"
